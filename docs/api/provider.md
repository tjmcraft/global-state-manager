# Provider API

```tsx
<Provider store={store} context?>{children}</Provider>
```

Provides store through React context.

## Props

- `store: Store<S, A>` required
- `context?: React.Context` optional custom GSM context
- `children: ReactNode`
